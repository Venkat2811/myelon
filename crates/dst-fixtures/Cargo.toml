[package]
name        = "dst-fixtures"
version     = "0.1.0"
description = "Internal: deterministic-simulation test fixtures shared by disruptor-mp and myelon integration tests."
publish     = false
edition.workspace      = true
license.workspace      = true
authors.workspace      = true
repository.workspace   = true
rust-version.workspace = true

[lints]
workspace = true

[lib]
name = "dst_fixtures"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
